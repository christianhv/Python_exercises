#!/usr/bin/env python
from sys import argv

script, first, second , third = argv

print "The name of the script is %s" % script
print "The first parameter is %s, the second is %s and the third is %s" % (first, second, third)